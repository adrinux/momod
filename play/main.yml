---
- hosts: all
  become: yes
  roles:
    - hostname
    - apt
    - unattended-upgrades
    - geerlingguy.ntp
    - sshd
    - common-packages
    - nginx-config
    - geerlingguy.firewall
    - powerdns.pdns_recursor
    - fail2ban

- hosts: dev
  become: yes

- hosts: prod
  become: yes
